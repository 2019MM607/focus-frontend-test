import React from 'react';

export const Fallback = () => {
    return (
        <div className="h-screen w-full bg-background text-red flex justify-center items-center">
            <h1>Loading...</h1>
        </div>
    );
};
